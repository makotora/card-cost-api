# card-cost-api
REST API to configure a clearing cost per country and request the cost for a specific card number according to its origin country.

The Card Cost API is implemented using the Spring Boot framework and Java 11.
Maven is used to manage dependencies and build the application whereas a running MySQL database is used for storage.

Check the Card_Cost_API.pdf document under the docs directory for installation instructions and implementation details.
